---
title: "Matrix Recursions: Spectral vs Numerical Radius"
format:
  html: default
  revealjs:
    html-math-method: mathjax
    width: 1920
    height: 1080
    mathjax: true
---

## The recursion $y_t = A y_{t-1}$

- **Closed form:**
  \[
  y_t = A^t\,y_0.
  \]

- **Two questions** drive the dynamics:
  1. Does $A^t \to 0$ (stability) or does it grow (instability)?
  2. Can $\lVert A^t\rVert$ exhibit **transient amplification** even if the system is asymptotically stable?

- The answers depend on *how we measure “size”* and on whether $A$ is **normal** (commutes with $A^*$) or **non-normal**.

---

## Spectral radius: the asymptotic growth rate

- **Definition (spectral radius):**
  \[
  \rho(A) := \max\{ |\lambda| : \lambda \in \sigma(A)\}.
  \]

- **Stability (discrete time):**
  \[
  \rho(A) < 1 \quad\Longleftrightarrow\quad A^t \to 0\ (t\to\infty).
  \]

- **Intuition:** eigenvalues give the long-run scaling of invariant “modes” (exactly for diagonalizable/normal matrices; asymptotically for all).

- **But:** eigenvalues alone can miss large **short-run** growth when $A$ is non-normal.

---

## Gelfand’s formula: estimating $\rho(A)$ from powers

For any matrix norm $\lVert\cdot\rVert$,
\[
\boxed{\ \rho(A) = \lim_{k\to\infty} \lVert A^k\rVert^{1/k}\ }
\]
(Gelfand’s formula)

**Interpretation**
- The quantity $\lVert A^k\rVert^{1/k}$ is an "average per-step growth factor" over $k$ steps.
- As $k$ increases, it filters out transients and converges to the true asymptotic rate $\rho(A)$.

**Practical note**
- Large $k$ can be numerically delicate (overflow/underflow); rescaling or working in Schur/QZ form is common.

---

## Power iteration: estimating the *spectral radius* (when it works)

The **classic** power iteration targets the eigenvalue of largest modulus, i.e. it can estimate
\(\rho(A)\) under standard conditions (dominant eigenvalue, start vector not orthogonal to its eigenvector).

**Algorithm (basic form)**
- Choose a nonzero vector \(v_0\).
- Iterate: \(w_{m+1} \leftarrow A v_m\), then normalize \(v_{m+1}\leftarrow w_{m+1}/\lVert w_{m+1}\rVert_2\).
- Track a scalar estimate (common choices):
  - **Growth ratio:** \(\lVert A v_m\rVert_2 / \lVert v_m\rVert_2\)
  - **Rayleigh quotient** (most meaningful when \(A\) is normal / Hermitian):
    \[
    \mu_m = \frac{v_m^* A v_m}{v_m^* v_m}.
    \]

**Convergence intuition**
- If \(|\lambda_1|>|\lambda_2|\) and \(A\) is diagonalizable, then \(v_m\) aligns with the dominant eigenvector and the estimates converge to \(|\lambda_1|=\rho(A)\).
- If the dominant eigenvalue is not unique in modulus (e.g. a complex conjugate pair), the iterates may oscillate.
- For strongly **non-normal** matrices, convergence can be slow and the Rayleigh quotient can be misleading; Schur/QZ-based methods are more robust.

**Important distinction**
- Power iteration applied to \(A^*A\) estimates \(\lVert A\rVert_2^2\) (largest singular value squared) — **not** \(\rho(A)\).

---

## Numerical radius: a one-step quadratic-form growth measure

- **Field of values (numerical range):**
  \[
  W(A) := \{ x^*Ax : \lVert x\rVert_2 = 1 \} \subset \mathbb{C}.
  \]

- **Numerical radius:**
  \[
  \boxed{\ w(A) := \max\{ |z| : z\in W(A)\} = \max_{\lVert x\rVert_2=1} |x^*Ax|\ }
  \]

**Key inequalities**
\[
\rho(A) \le w(A) \le \lVert A\rVert_2 \le 2\,w(A).
\]

**Intuition**
- $w(A)$ captures the largest possible “one-step gain” of the scalar quadratic form $x^*Ax$.
- For **normal** matrices: $\rho(A)=w(A)=\lVert A\rVert_2$.
- For **non-normal** matrices: often $\rho(A) \ll w(A)$.

---

## Why $w(A)$ matters for dynamics (transient growth)

Even when $\rho(A)<1$ (asymptotically stable), a non-normal matrix can have:

- **large $\lVert A\rVert_2$** (big one-step stretching)
- **large $w(A)$** (big one-step Rayleigh-quotient magnitude)
- **transient amplification**: $\lVert A^t\rVert$ can increase for some $t$ before eventually decaying.

A canonical 2×2 example:
\[
A = \begin{bmatrix}0.9 & M\\ 0 & 0.9\end{bmatrix},\quad M\gg 1.
\]

- Eigenvalues are both $0.9$ so $\rho(A)=0.9<1$.
- But for large $M$, $\lVert A\rVert_2$ (and typically $w(A)$) can be much larger than 1, producing sizeable short-run growth.

---

## Spectral vs numerical radius: the punchline

- **Spectral radius $\rho(A)$**
  - Governs *asymptotic* stability and long-run exponential rate.
  - Insensitive to how non-orthogonal the eigenvectors are.

- **Numerical radius $w(A)$**
  - Upper-bounds $\rho(A)$ and is closely tied to the geometry of the field of values.
  - More informative about *one-step* amplification and non-normality.

- **Rule of thumb**
  - If $A$ is (nearly) normal: $\rho(A)$ tells most of the story.
  - If $A$ is non-normal: expect transients; look at $w(A)$, $\lVert A\rVert_2$, and (if needed) pseudospectra.

---

## Computing $w(A)$ via a real-symmetric maximization

A very useful identity:
\[
\boxed{\ w(A) = \max_{\theta\in[0,2\pi)} \lambda_{\max}\!\Big(\operatorname{Re}(e^{-i\theta}A)\Big)\ }
\]
where
\[
\operatorname{Re}(e^{-i\theta}A) := \tfrac12\big(e^{-i\theta}A + e^{i\theta}A^*\big)
\]
is **Hermitian**.

**Intuition**
- Rotating $A$ by a phase $e^{-i\theta}$ rotates the field of values.
- Taking the real part selects the supporting line (tangent) of the rotated set.
- Maximizing the support function over $\theta$ recovers the farthest distance from the origin.

---

## Power iteration to estimate $w(A)$ (via a Hermitian reduction)

Because $H(\theta):=\operatorname{Re}(e^{-i\theta}A)$ is Hermitian, you can estimate
\(\lambda_{\max}(H(\theta))\) with **power iteration**.

*(This is **not** power iteration on \(A\) itself; it uses power iteration only as a subroutine to get the top eigenvalue of the Hermitian matrix \(H(\theta)\).)*

**Power iteration (Hermitian case)**
1. Pick a random unit vector $v_0$.
2. Repeat: $v_{m+1} \leftarrow H(\theta) v_m$ and normalize.
3. Rayleigh quotient: $\mu_m = v_m^* H(\theta) v_m$ converges to $\lambda_{\max}(H(\theta))$.

**Estimating $w(A)$**
- Choose a grid of angles $\theta_j$ on $[0,2\pi)$.
- For each $\theta_j$, run power iteration on $H(\theta_j)$ to get $\widehat\lambda_{\max}(\theta_j)$.
- Return
  \[
  \boxed{\ \widehat w(A) = \max_j\ \widehat\lambda_{\max}(\theta_j)\ }.
  \]

**Notes**
- Finer angle grids improve accuracy; adaptive refinement near the maximizing $\theta$ is common.
- For large sparse matrices, Lanczos typically outperforms plain power iteration.

---

## Connecting the two: radii and long-run behavior

- If you care about **eventual** decay/growth of $y_t=A^t y_0$:
  - the decisive quantity is $\rho(A)$ (via eigenvalues, or via Gelfand’s formula).

- If you care about **short-run** amplification, robustness, or sensitivity:
  - examine $w(A)$ and $\lVert A\rVert_2$ (and, for deeper analysis, pseudospectra).

- In many economic/DSGE linearizations:
  - $\rho(A)<1$ corresponds to stable laws of motion.
  - large $w(A)$ / $\lVert A\rVert_2$ can still mean substantial transient responses (large multipliers) even under stability.
